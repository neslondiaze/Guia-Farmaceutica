# Dependencias del proyecto (Python >= 3.10)
# La capa HTTP tiene triple fallback: curl_cffi -> curl CLI -> urllib,
# por lo que curl_cffi es opcional pero muy recomendable (evita bloqueos TLS).
curl_cffi>=0.7
beautifulsoup4>=4.12
